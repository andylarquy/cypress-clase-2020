import { OrderAlimentosPipe } from './order-alimentos.pipe';

describe('OrderAlimentosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderAlimentosPipe();
    expect(pipe).toBeTruthy();
  });
});
